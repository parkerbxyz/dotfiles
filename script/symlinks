#!/usr/bin/env bash
#
# Create symbolic links

GITHUB_USERNAME=$(git config github.user)

ln -s github.com/"$GITHUB_USERNAME"/exercism Exercism
ln -s ~/.dotfiles github.com/"$GITHUB_USERNAME"/dotfiles
ln -s .zsh/ .zsh
